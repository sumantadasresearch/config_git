# config_git
First time configuring git in linux <br>
I am using Ubuntu <br>
The command to test whether git is install or not : git <br>
The command to install git if it is absent        : sudo apt install git -y <br>
I have made a small change
